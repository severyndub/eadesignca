apiVersion: v1
kind: Service
metadata:
  name: wfa-service
  labels:
    name: wfa-service
spec:
  ports:
  - port: 8898
    protocol: TCP
    targetPort: 8898
  selector:
    app: wfa
  type: ClusterIP
